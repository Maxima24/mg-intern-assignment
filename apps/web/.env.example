# Base URL of OUR backend (never Setu). Include the /api prefix.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api

# This web app's own public origin (used to build the Setu redirectUrl).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
